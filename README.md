====== How to run ======
Open 2 terminals
In the first one run npm link
In the second one run cd src/TestApi, then node index.ts (Runs a dummy api to test the proxy)

Usage
cache-cli --port [PORT_NUMBER] --origin [ORIGIN_URL]
